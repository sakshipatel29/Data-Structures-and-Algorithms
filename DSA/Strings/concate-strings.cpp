#include<iostream>
#include<string>
using namespace std;

int main() {
    string str1 = "sakshi ";
    string str2 ="saifali ";
    string str3 = "are ";
    string str4 = "sweet ";
    string str5 = "sisters. ";
    string str6 = str1 + str2 + str3 + str4 + str5;
    cout<< str6<< endl;
    return 0;
}

// output: sakshi saifali are sweet sisters.
// because we have concated the strings using the + operator
// and the output is the concatenation of the strings
// the output is sakshi saifali are sweet sisters.
// because we have concated the strings using the + operator
// and the output is the concatenation of the strings
// the output is sakshi saifali are sweet sisters.